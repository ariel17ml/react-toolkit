import {Auth, AuthCallback} from './'

describe('Auth', () => {
  it('is truthy', () => {
    expect(Auth).toBeTruthy()
  })
})

describe('AuthCallback', () => {
  it('is truthy', () => {
    expect(AuthCallback).toBeTruthy()
  })
})